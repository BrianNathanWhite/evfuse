% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/naive_model.R
\name{fit_naive_model}
\alias{fit_naive_model}
\title{Fit a naive (single-source) 3-dim spatial model}
\usage{
fit_naive_model(
  stage1,
  dat,
  W_bs,
  D,
  source = c("NOAA", "ADCIRC"),
  lambda = 300,
  n_starts = 20,
  control = list(maxit = 2000, trace = 1)
)
}
\arguments{
\item{stage1}{An \code{evfuse_stage1} object from \code{\link{fit_gev_all}}
or \code{\link{fit_gev_detrended}}.}

\item{dat}{An \code{evfuse_data} object.}

\item{W_bs}{Raw bootstrap covariance matrix (L\emph{3 x L}3).}

\item{D}{Distance matrix (L x L).}

\item{source}{Character: "NOAA" or "ADCIRC".}

\item{lambda}{Wendland taper range in km (default 300).}

\item{n_starts}{Number of multi-start attempts (default 20).}

\item{control}{Control list passed to \code{optim}.}
}
\value{
An \code{evfuse_naive_model} object.
}
\description{
Fits a 3-dimensional GP with coregionalization to data from a single
source (NOAA or ADCIRC only). All sites observe all 3 parameters,
eliminating the partial-observation complexity of the joint model.
}
