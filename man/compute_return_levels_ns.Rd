% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/return_levels.R
\name{compute_return_levels_ns}
\alias{compute_return_levels_ns}
\title{Compute nonstationary return levels with covariate}
\usage{
compute_return_levels_ns(
  predictions,
  covariate_value,
  r = 100,
  alpha = 0.05,
  method = "both",
  n_sim = 2500,
  seed = NULL
)
}
\arguments{
\item{predictions}{An \code{evfuse_predictions} object from
\code{\link{predict_krig}} with 4-column \code{noaa_mean} and 4x4
covariance matrices.}

\item{covariate_value}{Scalar or vector (length n_new) of centered
covariate values at prediction sites.}

\item{r}{Return period in years (default 100).}

\item{alpha}{Confidence level for CIs (default 0.05 for 95\% CIs).}

\item{method}{One of \code{"delta"}, \code{"simulation"}, or
\code{"both"} (default).}

\item{n_sim}{Number of simulations (default 2500).}

\item{seed}{Random seed for simulation method.}
}
\value{
A data frame with columns: lon, lat, return_level, se_delta,
ci_lower_delta, ci_upper_delta, se_sim, ci_lower_sim, ci_upper_sim.
}
\description{
Takes kriging predictions of 4 GEV parameters (mu0, mu1, log_sigma, xi)
and a future covariate value, and computes r-year return levels with
confidence intervals via the delta method and/or simulation.
}
\details{
The effective location is \code{mu = mu0 + mu1 * covariate_value}, where
\code{covariate_value} should be centered relative to the same reference
used in \code{\link{fit_gev_ns}}.
}
