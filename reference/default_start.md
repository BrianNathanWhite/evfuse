# Generate default starting values

Generate default starting values

## Usage

``` r
default_start(stage1, dat, p = NULL)
```

## Arguments

- stage1:

  Stage 1 fits.

- dat:

  Data object.

- p:

  Dimension (default 6).

## Value

Named list with beta, A, rho.
