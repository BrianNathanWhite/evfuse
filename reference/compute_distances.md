# Compute pairwise great-circle distances between sites

Uses the Haversine formula. Returns distances in kilometers.

## Usage

``` r
compute_distances(sites)
```

## Arguments

- sites:

  Data frame with lon and lat columns (in degrees).

## Value

Symmetric distance matrix (km).
